import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "The Sapphire Pad | Dormitory in Katipunan",
  description: "Looking for a safe, cozy, and study-friendly dormitory in Katipunan near Ateneo and UP Diliman? Inquire now at The Sapphire Pad.",
  keywords: ["dormitory in katipunan", "student accommodation katipunan", "katipunan dorm", "the sapphire pad"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}